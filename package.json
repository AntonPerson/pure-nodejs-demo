{
  "name": "typescript-node-vanilla",
  "version": "1.0.0",
  "description": "A vanilla node project with typescript",
  "main": "src/index.js",
  "scripts": {
    "build:version": "ts-node build/version.ts",
    "prestart": "npm run build:version",
    "start": "ts-node src/index.ts",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^18.15.10",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2",
    "vitest": "^0.29.7"
  }
}
